# Build products and vendored libraries. `forge install` recreates lib/ and it
# is large; the README carries the exact install commands so a clone can
# reproduce it.
out/
cache/
lib/
broadcast/
.env

# remappings.txt is deliberately NOT ignored. If the build needs one, it is
# part of the working config and must travel with the repo — an untracked file
# passes every local gate and is missing from the release (CLAUDE.md §traps).
